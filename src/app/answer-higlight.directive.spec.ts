import { AnswerHiglightDirective } from './answer-higlight.directive';

describe('AnswerHiglightDirective', () => {
  it('should create an instance', () => {
    const directive = new AnswerHiglightDirective();
    expect(directive).toBeTruthy();
  });
});
